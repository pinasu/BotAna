        Easy ones (already working in BotAna 1.0):

1) Add message spam every x minutes and every n chatter's (not mods) messages;
2) Add new followers check every x minutes;
3) Add "ANA" at the end of user's name when he writes "ciao"/"buonasera"/etc in chat AND he's been in chat from x minutes;

        Medium ones (working in BotAna 1.0, but bugged af):
4) Fix Vodcast/Offline/Online BotAna status;

        Hard ones (make you wanna kill yourself instead):
5) Add new subscribers check;
6) Add new host check;
7) Add command (mod) to set stream title;
8) Add command (mod) to set stream current game;
